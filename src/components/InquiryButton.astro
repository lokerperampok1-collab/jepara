---
interface Props {
  href: string | null;
  label?: string;
}

const { href, label = "Tanya via Telegram" } = Astro.props;
---

{
  href && (
    <a
      href={href}
      target="_blank"
      rel="noreferrer"
      class="btn-primary"
    >
      {label}
    </a>
  )
}
