---
interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
  align?: "left" | "center";
}

const {
  eyebrow,
  title,
  description,
  align = "left",
} = Astro.props;
---

<div class:list={["max-w-2xl", align === "center" ? "mx-auto text-center" : ""]}>
  {eyebrow && (
    <p class="text-[0.72rem] font-semibold uppercase tracking-[0.28em] text-[color:var(--color-wood-deep)]">
      {eyebrow}
    </p>
  )}
  <h2 class="serif-display mt-3 text-2xl font-semibold leading-tight text-[color:var(--color-ink)] sm:text-3xl">
    {title}
  </h2>
  {description && <p class="mt-3 text-[0.98rem] leading-7 text-[color:var(--color-muted)]">{description}</p>}
</div>
